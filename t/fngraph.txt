# Copyright 2026 Leon Hwang.
# SPDX-License-Identifier: Apache-2.0

# These tests are for tracing function graph in the kernel.

# icmp_rcv
name: fngraph::icmp_rcv
tag: fngraph,pkt
test: -k '(g)icmp_rcv' --filter-pkt 'host 127.0.0.1' --fgraph-max-depth 3
match: ← ping_rcv args
trigger: ping -c 1 -s 64 127.0.0.1
